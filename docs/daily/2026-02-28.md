# 2026-02-28 日志

## 今日进度
- 状态：完成
- 今日完成：
  - 补齐结果页导出逻辑：Download 下拉支持 CSV / Excel(.xls XML) / JSON 三种下载。
  - QuickBooks 导出升级为专用 IIF 文件（`quickbooks-import.iif`）。
  - 补齐结果页数据持久化：从 `sessionStorage` 读取失败时回退 `localStorage`，并进行双写同步。
  - 底部操作条接入交互：Copy to Clipboard、Export to QuickBooks(IIF)、Start New Upload（清理存储并返回首页）。
  - 结果页成功提示条支持关闭。
  - 结果页无数据时改为空态页，不再展示 fallback 示例流水。
  - 完成离线构建：移除 `next/font/google` 依赖，改为本地 `woff2` 字体加载。
  - 完成 Figma 资源本地化：结果页使用的 14 个 SVG 图标/插图全部迁移到 `frontend/public/figma/results`。
  - 构建验证通过：`frontend` 下 `npm run build` 成功，无外网字体拉取失败。
- 关键修改点：
  - 上传成功后结果改为写入 `sessionStorage + localStorage`。
  - 新增导出转换工具函数（CSV 转义、Excel XML 转义、Blob 下载封装）。
  - 新增导出数据标准化 `exportRows`，优先使用解析结果交易数据。
  - `layout.tsx` 去除 Google Font 注入，`globals.css` 新增 `@font-face`（`InterLocal`/`ArimoLocal`）。
  - `results/page.tsx` 的 Figma 远程 URL 常量改为本地 `/figma/results/*.svg` 路径。
- 关联文件：
  - `frontend/src/app/page.tsx`
  - `frontend/src/app/results/page.tsx`
  - `frontend/src/app/layout.tsx`
  - `frontend/src/app/globals.css`
  - `frontend/public/fonts/inter-latin.woff2`
  - `frontend/public/fonts/arimo-latin.woff2`
  - `frontend/public/figma/results/*.svg`
- 已知问题/待验证：
  - QuickBooks 目前使用 IIF 通用导入路径，尚未覆盖 QBO 直连导入。
  - 仍存在 `next/no-img-element` lint warning（历史还原代码沿用 `<img>`）。

## 明日计划
- 若需要：评估将本地字体文件纳入稳定资源来源（避免依赖本机缓存来源）。
- 若需要：补充 E2E 检查，覆盖导出与空态路径。
