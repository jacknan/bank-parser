# 2026-02-27 日志

## 今日进度
- 状态：完成
- 今日完成：
  - 首页上传流程与后端对接，上传成功后写入 sessionStorage 并跳转结果页。
  - 新增解析成功结果页 `/results`，按 Figma 1:1 还原主视觉与表格。
  - 补齐底部浮动操作条（含 Copy/QuickBooks/Download/Start New Upload）。
  - Download 按钮下拉菜单（CSV/Excel/JSON）按截图还原。
- 关键修改点：
  - 新增前端 API 代理：`/api/convert` 转发至后端 `http://127.0.0.1:8080/api/convert`。
  - 结果页读取解析数据并动态渲染统计卡片与表格；分类列先统一显示 `Uncategorized`。
  - 底部操作条改为浮动 fixed。
- 关联文件：
  - `frontend/src/app/page.tsx`
  - `frontend/src/app/results/page.tsx`
  - `frontend/src/app/api/convert/route.ts`
- 已知问题/待验证：
  - 下载下拉菜单仅 UI 还原，导出逻辑未接入。
  - 结果页依赖 sessionStorage，刷新后结果会丢失（如需持久化需调整）。

## 明日计划
- 若需要：补齐下载导出逻辑（CSV/Excel/JSON）。
- 若需要：持久化结果页数据（localStorage/URL 参数/后端存储）。

## 备注
- Dev Server：`http://127.0.0.1:3001/`
- Backend：`http://127.0.0.1:8080/`
